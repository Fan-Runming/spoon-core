# Minimal requirements for Render deployment
# Core FastAPI dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
python-dotenv==1.0.1

# LLM providers
google-generativeai==0.8.4

# HTTP client for async requests
httpx==0.28.1

# Only include what's actually used by relationship_spark_agent
# These are the minimal dependencies from spoon_ai that are actually needed
anthropic==0.46.0
openai==1.59.8
requests==2.32.3
